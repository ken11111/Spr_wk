# バックアップファイル

このディレクトリには、古いバージョンのスクリプトが保管されています。

## 📂 ファイル一覧

| ファイル | 説明 | 置き換え先 |
|---------|------|-----------|
| **claude_executor_interactive.sh** | インタラクティブ実行版（手動実行） | `claude_executor.sh` (API自動実行版) |
| **queue_manager.sh** | キュー管理 v1 | `queue_manager_v2.sh` |
| **check_usage.sh** | 使用量チェック v1（1日/1時間制限） | `check_usage_v2.sh` (5時間ウィンドウ対応) |

## 🔄 変更履歴

### 2025-12-14: API自動実行版への移行

**変更理由**: 完全自動実行の要件を満たすため

**旧システム**:
- インタラクティブモード（手動実行が必要）
- NEXT_TASK.md にタスクを出力
- ユーザーがClaude CLIで手動実行

**新システム**:
- Claude API使用（完全自動）
- Python経由でAPI実行
- 結果を自動保存

## ⚠️ 注意

これらのファイルは参考用に保管されています。
現在のシステムでは使用されません。

削除しても問題ありませんが、念のため保管しています。

---

**バックアップ日**: 2025-12-14
